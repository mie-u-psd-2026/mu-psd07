# 会話全体の評価

目的は、開発時の質問文への一致ではなく、初めての相談でも判断に必要な条件を集め、初心者が選択肢で回答し、根拠のある推薦まで到達できるかを確認することです。

## 現在の方式と検証結果

「完全さより、例を活用して会話を進め、合わない質問はスキップする」方式に変更しました。ローカルのqwen3:8bを通常モードで使用します。有料API・外部AIへの送信は使っていません。

通常は1回のモデル呼び出しで条件整理を先に出力し、その後に質問・結論を生成します。条件整理・質問生成には、question_examples.jsonから関連する良い例を最大2件渡します。例が合わない相談では無理に使いません。これは実行時の参考例であり、モデルの追加学習ではありません。現在の相談で回答済み・スキップ済みの同じ文面は参考例からも除外します。

AIによる別の意味審査を、通常の会話を止める必須条件から外しました。形式不正、同一質問、スキップ文面、同じ選択肢、候補外の推薦、明白な結論の聞き返しは軽量なプログラム検証で修正します。意味の重複、説明の弱さ、選択肢の不十分さをすべて検出する仕組みではありません。従来の意味審査は `--strict-review`（アプリは `DECISION_STRICT_REVIEW=true`）で検証できます。

- 自動テスト51件とNodeによる画面状態の確認が通過。
- 実用優先モードの最初の5相談は、3回答後に結論を要求する手順ですべて結論に到達。全質問が選択式、各会話約38〜51秒。`evaluation-results/practical-validation.json` / `.md` に記録。
- この5相談の意味の自動採点は3件合格・2件要確認。旅行では結論を本人に選ばせる質問、通勤では説明の弱さが見られました。完走率を品質の合格率や「精度80%」とは扱いません。
- その後、明白な結論の聞き返しを軽量チェックに追加し、例を条件整理にも渡すように変更。旅行の再確認では「今の疲れ具合はいかがですか？」→「かなり疲れている」→「温泉旅行」と1回答で自然終了し、約16秒で完走。自動採点も合格。`evaluation-results/practical-examples-validation.json` / `.md` に記録。

実際に起動中のFlask APIでも、質問→その文面をスキップ対象にして別の質問→回答を添えて途中終了、の流れを確認しました（`evaluation-results/practical-http-smoke.json`）。各応答は約7〜9秒でした。結果の判断材料欄は、その後の修正で実際の回答原文から組み立てるようにし、自動テストで確認しています。

自動採点にも見落としがあります。上記の異なる実行を合算して80%達成と主張せず、実際の会話とスキップの使い勝手を見て例を改善してください。

「わからない」は4回連続で暫定終了を要求します。2回・3回だけを理由に自動終了しない境界はテスト済みです。

## 以前の厳格モードで確認した問題

8Bの非推論モードでは5相談とも審査で停止しました。8Bの推論モードでは完走する例がありましたが、選択肢や根拠の問題が残りました。14Bでは1回90〜130秒程度かかり、PCの相談は審査の誤判定を繰り返して180秒で時間切れになりました。これらの記録は `final-validation.json`、`thinking-validation.json`、`local-release.json` などに残しています。この結果を受け、通常の利用から必須の意味審査を外しました。

## 実行

Ollamaを起動してから、プロジェクトのルートで実行します。Flaskサーバーは不要です。

```powershell
python -m unittest discover -s backend -p "test_*.py"
node backend/test_frontend.cjs
python -m backend.evaluate --scenario language_lessons --scenario notes_unknown --max-turns 3 --output evaluation-results/smoke.json
python -m backend.evaluate --split all --repeat 2 --output evaluation-results/full.json
```

実モデル評価は時間とローカルGPU/CPUを使用します。標準は各相談5回答まで、それ以降は途中終了を要求します。`--max-turns 15`で通常の質問上限まで試せます。自然終了と途中終了を記録上で区別します。

高負荷時に品質と速度を分けて調べる場合は`--request-timeout 300`で評価時だけ期限を延長できます。通常のアプリの期限は変更しません。延長した評価で成功しても、通常の応答時間を満たしたとは扱わず、JSONに記録された期限と各ターンの時間を確認してください。

GPUを使えない環境では実行するシェルだけで`$env:OLLAMA_NUM_GPU='0'`を指定するとCPUで推論できます。解除は`Remove-Item Env:OLLAMA_NUM_GPU`です。CPUでも負荷は発生し、応答が遅くなる場合があります。

GPUの空きメモリが少ない場合は`$env:OLLAMA_NUM_GPU='16'`などで一部の層だけをGPUへ配置できます。これはモデルと端末に依存する設定です。アプリにも適用する場合は、その環境変数を指定したシェルからサーバーを起動してください。

生成・審査のモデルは`--model`、会話の回答者と会話全体の評価モデルは`--judge-model`で指定できます。既定は両方とも`qwen3:8b`です。モデルは事前にローカルへ取得してください。

## 評価データ

`backend/eval_scenarios.json`に、相談、利用者の設定、回答モード、区分を記載しています。生成・審査側に利用者の設定や評価結果は渡しません。生成側が受け取るのは相談と、実際に質問して得た回答だけです。相談文と回答済みの条件は原文をそのまま保持し、既知の事情をAIに再要約させません。AIは候補と未確認条件を整理します。厳格モードの審査担当には過去の不合格理由を渡さず、今回の案を独立に評価させます。

- `regression`：既存のPC・旅行の相談。既存体験の退行を調べる。
- `heldout`：英会話、読書記録、洗濯、作業場所、3候補の運動など、質問例を本体に埋め込んでいない相談。
- `answer_mode: unknown`：すべて「わからない・決められない」と回答し、再質問、不明情報の捏造、終わらない会話を調べる。

人物設定にない事実は評価用回答者も作らず、不明として返します。選択式では表示された選択肢に一致するかをプログラムでも検証します。数値・自由入力の質問にも画面から不明を回答できます。

`heldout`も結果を見て開発に使った時点で、厳密には未知の評価データではなくなります。継続的な評価では別の担当者が新しい相談を作り、`--scenarios 別のJSONファイル`で読み込んでください。参考例として採用した相談は、未知の相談の評価から外し、回帰確認として扱ってください。

## 合格基準と人による確認

会話全体を次の5軸で1〜5点評価し、すべて4以上かつ結果まで到達し、通信・品質エラーがないことを自動評価の合格基準にします。

| 軸 | 確認する内容 |
|---|---|
| 質問の有用性 | 回答が候補の判断に影響するか。結論を利用者に丸投げしていないか |
| 答えやすさ | 専門知識や調査が不要で、質問が1つに絞られているか |
| 重複の回避 | 既知情報や「わからない」と答えた内容を聞き直していないか |
| 結論の根拠 | 実際の回答と整合し、不明情報を補わず、他候補との比較があるか |
| 選択式の優先 | ボタンで回答できるか。数値・自由入力の例外に必要性があるか |

選択式率、各ターンの秒数、再生成内容、自然終了／途中終了も記録します。選択式率だけを最大化して無意味な選択肢を出すのは改善とは扱いません。

自動評価モデルも間違えます。同じモデルを使う場合は共通の見落としがあります。結果のMarkdownから会話を読み、特に「選択肢が質問への答えか」「重要な未確認条件が残るのに終了していないか」「回答と結論の因果関係」を人が確認してください。別モデルの評価は補助になりますが、人の確認の代わりにはなりません。

JSONには計画・生成案・審査・失敗が記録され、Markdownには会話と評価がまとまります。実データで評価した場合は相談内容が保存されるので、共有範囲を確認してください。

## 通信と実行時間

通常は計画を先に含む1回、修正を含め最大2回のモデル呼び出しです。厳格モードでは審査も含め通常3回・最大9回です。質問が不合格の場合は、計画内の別の未確認条件を先に試し、毎回同じ条件を再生成することを避けます。1回の画面操作全体で180秒の期限を共有し、画面側は190秒で中断します。通常モードでは意味の審査を必須にしませんが、形式検証を省いて返すことはありません。

履歴と審査の入力を収めるため、Ollamaのnative APIで`num_ctx=8192`を指定しています。`OLLAMA_NUM_CTX`で変更できますが、小さくすると長い相談の評価が失敗しやすくなります。メモリ使用量と速度は端末によります。既存のOpenAI SDKはHTTP通信と例外処理に使用しています。[Ollama公式の文脈長設定](https://docs.ollama.com/faq)、[OpenAI互換APIの文脈長の制約](https://docs.ollama.com/api/openai-compatibility)に基づく構成です。

既定はqwen3:8bの非推論モード、temperature=0.7、top_p=0.8、top_k=20、min_p=0です。`OLLAMA_THINK=true`で推論を有効にできますが、応答時間が増えます。評価JSONには推論設定とstrict_reviewを記録します。

## 犬・猫の質問と待ち時間の修正

動物の習性を利用者に尋ねる質問を検出し、本人の住居・留守時間・世話・留守中の助けを聞く参考例を追加。回答済みの条件に対応する参考例も除外します。通常モードは計画と選択式質問を1回で生成し、モデルを15分保持します。

再起動後の実HTTP確認では、犬猫とも飼育可能・留守4〜8時間という代表的な2回答の履歴に対して「泊まりで留守にするとき、世話を頼める相手はいますか？」を6.06秒で返しました。初回の質問は3.48秒。元の利用者の2回答は不明なため、同一履歴の再現ではありません。少数の測定であり、初回モデル読み込みや負荷時も20秒以内という保証ではありません。記録: evaluation-results/pet-one-call-smoke.json。上記5相談の評価は変更前の方式による過去の結果です。
