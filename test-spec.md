# テスト仕様書: AI意思決定サポーター

## 1. テスト対象

- `backend/app.py`（Flaskサーバー / APIバックエンド）
- `frontend/index.html`（Vue.js フロントエンドUI）
- Ollamaによる生成AI応答処理
- AIによる質問形式・選択肢・最終結果の生成処理

---

## 2. テスト環境

- Windows 10 / 11
- Python 3.13
- Flask
- openai
- Vue.js CDN版
- Ollama
- 使用モデル: `gemma3:4b`
- ブラウザ:
  - Chrome
  - Edge
  - Firefox

---

## 3. テスト項目

### 3.1 サーバー起動・静的ファイル配信

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 1 | サーバー起動 | `python backend/app.py`で起動 | エラーなく起動し、`http://localhost:5000/`でアクセス可能 |
| 2 | index.html配信 | ブラウザで`/`にアクセス | AI意思決定サポーターのトップ画面が表示される |
| 3 | Vue.js動作確認 | トップ画面を表示 | 入力欄・「相談する」ボタンなどが正常に操作できる |
| 4 | Ollama接続確認 | Ollamaを起動した状態で相談を送信 | AIから正常に応答が返る |

---

### 3.2 相談入力機能

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 5 | 相談内容入力 | 「ノートPCとデスクトップPCで迷っている」と入力 | 入力内容が正しく保持される |
| 6 | 相談開始 | 相談内容を入力して「相談する」を押す | AIへのリクエストが実行される |
| 7 | 日本語入力 | 日本語で相談内容を入力 | 文字化けせずAIへ送信される |
| 8 | 長めの相談入力 | 複数文で相談内容を入力 | 入力内容全体がAIへ送信される |
| 9 | 空文字送信 | 何も入力せず「相談する」を押す | APIを実行せず「相談内容を入力してください」などのエラーを表示する |

---

### 3.3 AI質問生成機能 正常系

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 10 | 最初の質問生成 | PC選びについて相談を開始 | 意思決定に必要な質問が1つ表示される |
| 11 | 質問数確認 | AIから質問を取得 | 一度に複数ではなく1つだけ質問される |
| 12 | 適切な質問生成 | PC選びについて相談 | 予算・用途・持ち運びなど、相談内容に関係する質問が生成される |
| 13 | 回答履歴利用 | AIの質問に回答して次へ進む | 前の回答を踏まえた次の質問が生成される |
| 14 | 重複質問防止 | 複数回質問に回答 | すでに回答した内容を同じ形で再度質問しない |
| 15 | 別ジャンル対応 | 「車を買うかカーシェアにするか」で相談 | PCとは異なる内容に適した質問が生成される |
| 16 | 確信度による終了 | AIとの質問を続ける | 確信度が100%になったところで質問を終了し、結果生成へ進む|

---

### 3.4 選択式回答機能

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 17 | 選択式生成 | AIが`answer_type: choice`を返す | 選択肢がボタンとして表示される |
| 18 | 選択肢数 | 選択式質問を表示 | 2～5個程度の選択肢が表示される |
| 19 | ボタン回答 | 任意の選択肢をクリック | クリックした内容がユーザーの回答として送信される |
| 20 | 送信ボタン不要 | 選択肢ボタンをクリック | 別の送信ボタンを押さなくてもAIリクエストが開始される |
| 21 | 回答履歴表示 | 選択肢を回答 | 選択した内容が会話履歴に表示される |
| 22 | Yes / No質問 | 二択で回答できる質問を生成させる | 「はい」「いいえ」などのボタンが表示される |
| 23 | その他ボタン | 選択肢に「その他」がある場合に押す | テキスト入力欄が表示される |

---

### 3.5 数値入力機能

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 24 | 数値入力表示 | AIが`answer_type: number`を返す | 数値入力フォームが表示される |
| 25 | 単位表示 | 予算について質問される | 「円」など適切な単位が入力欄の近くに表示される |
| 26 | 正常な数値入力 | `100000`などを入力し「決定」 | 数値がユーザー回答としてAIへ送信される |
| 27 | 空欄 | 数値を入力せず「決定」 | 送信されず、入力を促すメッセージが表示される |
| 28 | 数字以外の入力 | 数値欄に文字を入力しようとする | 不正な値が送信されない |

---

### 3.6 テキスト入力機能

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 29 | テキスト入力表示 | AIが`answer_type: text`を返す | テキスト入力欄が表示される |
| 30 | 自由入力 | 「3DCG制作にも使いたい」などを入力 | 入力内容がAIへ送信される |
| 31 | その他入力 | 「その他」ボタンから自由入力 | 入力内容が回答として会話履歴に追加される |
| 32 | 空文字 | テキスト欄を空で送信 | 送信されず入力を促すメッセージを表示する |

---

### 3.7 最終結果生成機能

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 33 | 結果画面遷移 | 必要な情報が揃うまで回答する | 質問画面から結果画面へ切り替わる |
| 34 | おすすめ表示 | 最終結果を取得 | おすすめの選択肢が表示される |
| 35 | 判断条件表示 | 最終結果を取得 | 判断に使用したユーザー条件が表示される |
| 36 | 理由表示 | 最終結果を取得 | なぜその選択肢をおすすめするのか表示される |
| 37 | メリット表示 | 最終結果を取得 | おすすめのメリットが表示される |
| 38 | デメリット表示 | 最終結果を取得 | おすすめのデメリットが表示される |
| 39 | 別条件表示 | 最終結果を取得 | 他の選択肢が向いている条件が表示される |
| 40 | 回答との整合性 | 「頻繁に持ち運ぶ」と回答してPC選びを完了 | ユーザー回答を無視した不自然な結果になっていない |
| 41 | 確信度ゲージ表示 | 質問画面へ進む | 画面下部に0〜100%の確信度ゲージが表示され、回答ごとに値が増える |
| 42 | 確信度100%で終了 | 確信度が100%になるまで回答する | 質問が終了し、結果画面へ切り替わる |
| 43 | 途中終了 | 質問中に「ここで結論を出す」を押す | その時点までの回答で結果が生成され、結果画面へ切り替わる |

---

### 3.8 APIレスポンス形式

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 44 | 選択式JSON | 選択式質問を生成 | `status`, `question`, `answer_type`, `options`, `confidence`が含まれる |
| 45 | 数値式JSON | 数値質問を生成 | `status`, `question`, `answer_type`, `unit`, `confidence`が含まれる |
| 46 | テキスト式JSON | 自由入力質問を生成 | `status`, `question`, `answer_type`, `confidence`が含まれる |
| 47 | 結果JSON | 最終結果を生成 | `recommendation`, `conditions`, `reason`, `pros`, `cons`, `alternative`, `confidence`が含まれる |
| 48 | status判定 | 質問と結果のAPI応答を確認 | 質問時は`question`、結果時は`result`が返る |

---

### 3.9 API・AI異常系

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 49 | 必須項目なし | 相談内容を含めずAPIへ送信 | 必須入力がないことを示すエラーが返る |
| 50 | Ollama未起動 | Ollamaを停止した状態で相談 | AIとの通信に失敗した旨を表示する |
| 51 | モデル未取得 | `gemma3:4b`が存在しない状態で実行 | モデルを利用できないことを示すエラーが表示される |
| 52 | AI応答なし | AIから空の応答が返った場合 | アプリが停止せず、AI応答を取得できなかった旨を表示する |
| 53 | JSON形式異常 | AIが期待するJSON形式以外を返す | アプリが異常終了せずエラーとして処理する |
| 54 | answer_type異常 | AIが未定義の`answer_type`を返す | エラー表示またはテキスト入力などの代替処理を行う |
| 55 | 通信エラー | AIとの通信が失敗 | 「AIとの通信に失敗しました」などのエラーを表示する |
| 56 | 入力内容保持 | AI通信エラーを発生させる | ユーザーが入力・選択した内容が消えない |
| 57 | 再送信 | 通信エラー後に再実行 | 同じ回答からAIへのリクエストを再実行できる |

---

### 3.10 UIテスト

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 58 | トップ画面 | `/`へアクセス | アプリ名、相談入力欄、相談ボタンが表示される |
| 59 | 質問回数表示 | 質問画面へ進む | `質問 1` など現在の質問回数が表示される |
| 60 | 処理中表示 | AIへのリクエストを実行 | 「AIが考えています…」などが表示される |
| 61 | 多重送信防止 | AI処理中に回答ボタンを連打 | 複数回リクエストされない |
| 62 | ボタン表示 | 選択式質問 | 全選択肢が押しやすいボタンで表示される |
| 63 | 会話履歴表示 | 複数回回答する | AIの質問とユーザーの回答を区別して表示できる |
| 64 | 結果表示 | 最終結果が正常に返る | 結果内容が項目ごとに読みやすく表示される |
| 65 | エラー表示 | 通信エラーを発生させる | エラーメッセージが画面上に表示される |
| 66 | 再相談 | 結果画面で「別の相談をする」を押す | 最初の相談入力画面に戻る |
| 67 | 初期化確認 | 「別の相談をする」を押す | 前回の質問・回答・結果が初期化される |

---

## 4. 代表的なシナリオテスト

### 4.1 PC選び

#### 入力

```text
大学用のPCをノートPCにするか
デスクトップPCにするか迷っている
```

#### 想定するやり取り

1. AI  
   「大学へPCを持ち運ぶ頻度はどのくらいですか？」

   - ほぼ毎日
   - 週に数回
   - 月に数回
   - ほとんどない

2. ユーザー  
   「週に数回」

3. AI  
   「主な用途は何ですか？」

   - レポート・Web閲覧
   - プログラミング
   - 動画・画像編集
   - ゲーム
   - その他

4. ユーザー  
   「プログラミング」

5. AI  
   「PCに使える予算はいくらですか？」

6. ユーザー  
   `100000円`

#### 期待結果

- AIが収集した条件を利用して判断する
- 「ノートPC」などのおすすめが表示される
- おすすめする理由が表示される
- メリット・デメリットが表示される
- デスクトップPCが向いている条件も表示される

---

### 4.2 別ジャンルの相談

#### 入力

```text
車を買うかカーシェアを利用するか迷っている
```

#### 期待結果

PC相談用の質問ではなく、

- 車を利用する頻度
- 1回の利用時間
- 月に使える費用
- 駐車場の有無

など、**相談内容に応じた異なる質問が生成されること**を確認する。

---

## 5. その他

以下についても必要に応じて確認する。

- 相談内容の最大入力文字数
- 自由入力回答の最大文字数
- 大きな数値を入力した場合の動作
- 負の数を入力した場合の動作
- 日本語以外で相談した場合の動作
- AI応答までの時間
- 異なる相談ジャンルで質問内容が変化するか
- 確信度が100%になったら質問が終了するか（質問回数に上限はない）
- 途中終了ボタンでその時点の回答から結果が生成されるか
- 「その他」を選んだときに自由入力欄が表示されるか
- 同一内容を繰り返し質問しないか
- ボタン選択を優先して生成しているか
- 金額・時間など必要な場合のみ数値入力になるか
- テキスト入力が必要以上に使用されていないか

---

## 6. 合格基準

以下を満たした場合、主要機能のテストを合格とする。

- サーバーが正常に起動する
- トップ画面が正常に表示される
- 相談内容をAIへ送信できる
- AIが相談内容に応じた質問を生成できる
- 原則として選択肢をボタンで回答できる
- 必要な場合に数値入力・テキスト入力へ切り替わる
- 回答履歴を踏まえて次の質問が生成される
- 同じ質問を繰り返さない
- 確信度が100%になったら、または途中終了で結果が表示される
- 質問終了後におすすめと判断理由を表示できる
- AI通信エラーが発生してもアプリが異常終了しない
- 「別の相談をする」で初期状態へ戻れる